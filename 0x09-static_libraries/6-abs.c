#include "main.h"
#include <stdlib.h>
#include <math.h>

/**
 * _abs - checks the absolute value of an integer
 * @x: is an integer to be checked
 *
 * Return: returns sbsolute value of x
 */


int _abs(int x)
{


	if (x > 0)
		return (x);
	else
		return (-x);
}
